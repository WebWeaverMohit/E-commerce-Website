<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wishlist</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">

<nav class="bg-gray-800 p-4">
    <div class="container mx-auto flex justify-between items-center">
        <a href="#" class="text-white font-bold text-lg">E-commerce App</a>
        <ul class="flex space-x-4">
            <li><a href="/profile" class="text-white">Profile</a></li>
            <li><a href="/logout" class="text-white">Logout</a></li>
        </ul>
    </div>
</nav>

<div class="container mx-auto mt-8">
    <h1 class="text-2xl font-bold mb-4">Your Wishlist</h1>
    <% if (wishlist && wishlist.items && wishlist.items.length > 0) { %>
        <div class="grid w-52 grid-cols-1 gap-4">
            <% wishlist.items.forEach(function(item) { %>
                <div class="bg-white rounded-lg shadow-md overflow-hidden">
                    <img src="/images/uploads/<%= item.productId.image %>" class="w-full h-48 object-cover">
                    <div class="p-4">
                        <h3 class="font-bold text-lg mb-2"><%= item.productId.productName %></h3>
                        <p class="text-gray-600 mb-2"><%= item.productId.description %></p>
                        <p class="text-gray-600 mb-2">$<%= item.productId.price %></p>
                    </div>
                </div>
            <% }); %>
        </div>
    <% } else { %>
        <p>Your wishlist is empty</p>
    <% } %>
</div>

</body>
</html>
